# index.-html
# Inicializar repo
git init
git add .
git commit -m "Initial commit: CellNet Security App"

# Crear repo en GitHub primero, luego:
git branch -M main
git remote add origin https://github.com/TU_USUARIO/cellnet-security.git
git push -u origin main
